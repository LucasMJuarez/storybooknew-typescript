import {style} from 'typestyle';
import {colors} from '../../../styles';
const styles = {
    container: style({
        padding: '3px 21px',
        borderRadius: '3px',
        backgroundColor: colors.android_bars_primary_primary_dark,
        width: 'max-content',
    }),
};

export default styles;
