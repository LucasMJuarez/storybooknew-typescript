import {style} from 'typestyle';

const styles = {
    container: style({
        padding: '16px 0',
        margin: '0px',
        fontWeight: 'normal',
        marginBlock: '0px',
    }),
};

export default styles;
