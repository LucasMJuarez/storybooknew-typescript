import {style} from 'typestyle';

const styles = {
    container: style({
        padding: '6px 24px 6px 16px',
        display: 'flex',
    }),
    iconContainer: style({
        marginRight: '8px',
    }),
};

export default styles;
