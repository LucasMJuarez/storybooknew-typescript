import React from 'react';
import AlertError from './AlertError';

export default {
    title: 'Assets/24/AlertError',
    component: AlertError,
};

export const AlertErrorIcon = () => <AlertError />;
