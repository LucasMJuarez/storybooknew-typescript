import {style} from 'typestyle';
const styles = {
    container: style({
        width: "24px",
        height: "24px",
        display: "grid",
        placeItems: "center"
    }),
};

export default styles;
