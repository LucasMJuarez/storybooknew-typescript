import {style} from 'typestyle';
const styles = {
    container: style({
        width: "32px",
        height: "32px",
        display: "grid",
        placeItems: "center"
    }),
};

export default styles;
