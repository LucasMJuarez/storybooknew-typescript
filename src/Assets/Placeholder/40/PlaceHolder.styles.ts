import {style} from 'typestyle';
const styles = {
    container: style({
        width: "40px",
        height: "40px",
        display: "grid",
        placeItems: "center"
    }),
};

export default styles;
