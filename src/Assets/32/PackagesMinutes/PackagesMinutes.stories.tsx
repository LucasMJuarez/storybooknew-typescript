import React from 'react';
import PackagesMinutes from './PackagesMinutes';

export default {
    title: 'Assets/32/PackagesMinutes',
    component: PackagesMinutes,
};

export const PackagesMinutesIcon = () => <PackagesMinutes />;
