import { style } from 'typestyle';

const styles = {
    container: style({
        height: 32,
        paddingRight: '16px',
        paddingLeft: '16px',
        display: 'flex',
        alignItems: 'center',
    }),
};

export default styles;
