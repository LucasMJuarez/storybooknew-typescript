import {style} from 'typestyle';

const styles = {
    container: style({
        padding: '6px 24px 6px 16px',
        cursor: 'pointer',
        maxWidth: '180px',
    }),
};

export default styles;
