import { style } from 'typestyle';

const styles = {
    container: style({
        padding: '11px 16px 13px 16px',
    }),
    link: style({
        color: '#00a9e0',
        textDecoration: 'none',
    }),
};

export default styles;
