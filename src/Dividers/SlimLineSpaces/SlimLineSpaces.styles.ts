import { style } from 'typestyle';
import { colors } from '../../styles';

const styles = {
    SlimLineSpaces: style({
        width: 'auto',
        height: 1,
        backgroundColor: colors.grey_4,
    }),
};

export default styles;
