import {style} from 'typestyle';
import {colors} from '../../styles';

const styles = {
    SlimLineFullWidth: style({
        width: '100%',
        height: 1,
        backgroundColor: colors.android_dividers_divider,
    }),
};

export default styles;
